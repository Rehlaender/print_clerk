%table.totals
  %tr
    %td.cel1.bold
      = t(:purchase_total)
      \:
    %td.cel2.bold{:align => "right"}= euros(@purchase.basket.total_price)
  %tr
    %td
      &nbsp;
    %tr.hide
      %td.cel1.hide
        = "#{t(:vat)}:"
      %td.cel2.hide{:align => "right"}= euros(@purchase.basket.total_tax)
